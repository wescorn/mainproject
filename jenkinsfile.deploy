pipeline {
    agent any
    parameters {
        string defaultValue: "", name: 'DEPLOY_NUMBER', trim: true
    }
    stages {
        stage("Deploy") {
            steps {
                sh "docker compose rm orders versiontwo"
                sh "docker compose up orders versiontwo"
            }
        }
    }
}